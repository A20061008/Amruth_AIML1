// Online C compiler to run C program online
#include <stdio.h>

int main() {
int Num;
printf("enter the integer:");
scanf("%d",&Num);
if(Num>0)
printf("num is possitive");
else
printf("num is negative");
    return 0;
}